#!/bin/bash
# Simulated 'ls' for /
if [[ "$1" == "/" || "$1" == "" ]]; then
  echo "bin  boot  dev  etc  home  lib  proc  root  tmp  usr  var"
else
  echo "ls: cannot access '$1': No such file or directory"
fi